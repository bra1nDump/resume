{
  "scripts": {
    "watch": "npx chokidar '*.md' 'style.css' 'template.html' --command 'npm run build'",
    "build": "./build.sh"
  },
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "html-pdf-chrome": "^0.8.4"
  }
}